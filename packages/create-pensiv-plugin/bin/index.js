#!/usr/bin/env node
/**
 * create-pensiv-plugin — scaffolds a new plugin from `plugins/sample-plugin`.
 *
 * Placeholder (Phase 2). For now, copy `plugins/sample-plugin/`, rename it, and
 * change the `id` in `manifest.json` to your own reverse-DNS namespace.
 */
console.log(
  [
    'create-pensiv-plugin is not implemented yet.',
    '',
    'For now, scaffold by hand:',
    '  1. Copy plugins/sample-plugin/ to your new plugin folder.',
    '  2. Edit manifest.json — set a unique reverse-DNS `id` and your name.',
    '  3. Edit src/main.ts.',
    '  4. npm install && npm run build.',
    '',
    'Docs: https://github.com/pensiv-so/pensiv-plugins/blob/main/AGENTS.md'
  ].join('\n')
);
