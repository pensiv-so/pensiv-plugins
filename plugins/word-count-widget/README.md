# Word Count Widget

A floating widget showing the live word count of the active document.

## Features

- Always-visible word count that updates as you type
- Follows whichever document is active
- Stays out of the way — a small floating widget, no pane or panel

## Usage

Enable the plugin and the widget appears while a document is open, counting as you write.
