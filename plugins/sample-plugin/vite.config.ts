import { definePluginConfig } from '@pensiv/build-config';

// Builds to dist/main.js (+ dist/styles.css if you add CSS), with react and
// @pensiv/plugin-sdk left external for the host to provide.
export default definePluginConfig();
