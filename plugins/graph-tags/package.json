{
  "name": "@pensiv-plugins/graph-tags",
  "version": "1.0.0",
  "private": true,
  "description": "Relationship-graph example — tag overlay, filter, toolbar and node menu.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@pensiv/plugin-sdk": "*"
  },
  "devDependencies": {
    "@pensiv/build-config": "*",
    "vite": "^6.0.0"
  }
}
